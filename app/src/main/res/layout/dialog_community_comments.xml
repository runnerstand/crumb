<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_cream"
    android:orientation="vertical"
    android:padding="@dimen/dialog_padding"
    tools:context=".CommunityCommentsDialogFragment"
    tools:theme="@style/Theme.Crumb">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/comments_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/community_comments_title"
        android:textColor="@color/text_dark_brown"
        android:textSize="22sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_medium"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/comment_status_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/community_comments_loading"
            android:textColor="@color/text_muted_brown"
            android:textSize="14sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/add_comment_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/add_comment"
            app:cornerRadius="@dimen/button_radius" />
    </LinearLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/comment_retry_button"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_small"
        android:text="@string/retry"
        android:textColor="@color/primary_orange"
        android:visibility="gone"
        app:cornerRadius="@dimen/button_radius"
        app:strokeColor="@color/primary_orange"
        tools:visibility="visible" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/comments_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="@dimen/spacing_small"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:visibility="gone"
        tools:itemCount="3"
        tools:listitem="@layout/item_community_comment"
        tools:visibility="visible" />
</LinearLayout>
